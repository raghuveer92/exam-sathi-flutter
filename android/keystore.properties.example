# keystore.properties — GITIGNORED, DO NOT COMMIT
# Copy this file to android/keystore.properties and fill in real values.
#
# For CI/CD, set these as GitHub Secrets instead:
#   KEYSTORE_BASE64  — base64-encoded .jks file
#   KEY_ALIAS        — key alias
#   KEY_PASSWORD     — key password
#   STORE_PASSWORD   — keystore password

storeFile=../app/keystore.jks
storePassword=examsathi12345
keyAlias=examsaathi
keyPassword=examsathi12345
